services:
  sequencer:
    image: ${ROFL_APP_IMAGE:-sequencer:local}
    restart: unless-stopped
    env_file:
      - .env
    environment:
      NODE_ENV: production
      BATCHER_PRIVATE_KEY: ${BATCHER_PRIVATE_KEY}
    volumes:
      - /run/rofl-appd.sock:/run/rofl-appd.sock

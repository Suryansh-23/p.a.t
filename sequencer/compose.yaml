services:
  sequencer:
    image: ${ROFL_APP_IMAGE:-sequencer:local}
    restart: unless-stopped
    env_file:
      - .env
    environment:
      NODE_ENV: production
    volumes:
      - /run/rofl-appd.sock:/run/rofl-appd.sock
